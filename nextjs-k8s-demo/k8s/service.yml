apiVersion: v1
kind: Service
metadata:
  name: nextjs-service
  namespace: nextjs-demo
spec:
  selector:
    app: nextjs
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
